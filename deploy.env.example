# Optional manual overrides for deploy.sh
# Copy to deploy.env if you prefer not to read values from Terraform state:
#   cp deploy.env.example deploy.env

BUCKET="your-site-bucket-name"
DISTRIBUTION_ID="E1234567890ABC"
